import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.shibapp.starter',
  appName: 'shibapp',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
